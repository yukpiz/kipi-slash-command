machine:
  python:
    version: 2.7.10
  environment:
    AWS_LAMBDA_ROLE: "arn:aws:iam::793539351801:role/lamvery-test"
dependencies:
  pre:
    - pip install lamvery
test:
  override:
    - echo 'test'
deployment:
  master-head:
    branch: master
    commands:
      - lamvery deploy